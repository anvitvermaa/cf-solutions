import sys input = sys.stdin.readdata = input().split()index = 0t = int(data[index])index += 1results = [] for _ in range(t):    n = int(data[index])    x = int(data[index + 1])    index += 2        points_list = [0]        for _ in range(n):        point = int(data[index])        points_list.append(point)        index += 1            points_list.append(x)    n = len(points_list)    max_distance_between_points = -float('inf')        for i in range(1, n):        if i == n - 1:            max_distance_between_points = max(max_distance_between_points, 2 * (points_list[i] - points_list[i - 1]))        else:            max_distance_between_points = max(max_distance_between_points, points_list[i] - points_list[i - 1])                results.append(max_distance_between_points) for result in results:    print(result)